# Maintenance Scripts

Older maintenance and one-off operational utilities that are still kept with the repo:

- Pinecone / index cleanup helpers
- PDF ingestion helpers
- URL cleanup / validation utilities
- image extraction helpers

These are not part of the normal product-quality test path.

Run them from the repo root so their relative paths and `.env` loading still behave as expected.
